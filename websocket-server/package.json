{
  "name": "ai-tutor-minimal-websocket",
  "version": "1.0.0",
  "description": "Minimal WebSocket server for AI tutor streaming",
  "main": "minimal-server.ts",
  "scripts": {
    "dev": "npx tsx watch minimal-server.ts",
    "start": "npx tsx minimal-server.ts",
    "build": "npx tsc minimal-server.ts --outDir dist",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "ws": "^8.14.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/ws": "^8.5.9",
    "@types/jsonwebtoken": "^9.0.5",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 